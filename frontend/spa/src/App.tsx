import { Toolbar } from "@mui/material";
import { FC } from "react";
import { TopBar } from "./topbar";

export const App: FC = () => {
  return (
    <div>
      <TopBar />
      <main>hi</main>
    </div>
  );
};
