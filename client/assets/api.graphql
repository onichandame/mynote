query users {
  users {
    edges {
      node {
        id
        name
        email
        avatar
        createdAt
        updatedAt
      }
    }
  }
}

mutation login($identity: String!, $password: String!) {
  login(input: { identity: $identity, password: $password })
}
